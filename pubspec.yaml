name: supwave_gen2
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Full Material 3 Expressive component set (buttons, nav, progress, etc.),
  # built on package:material_ui instead of package:flutter/material.dart.
  # Brings its own dynamic_color / motor / material_new_shapes deps.
  material_3_expressive: ^1.1.2

  # Declared explicitly since every screen imports it directly in place of
  # package:flutter/material.dart (required by material_3_expressive).
  # Pinned: newer material_ui releases use @awaitNotRequired, which our
  # installed stable Flutter's foundation.dart doesn't re-export yet.
  material_ui: 1.1.1

  # Same @awaitNotRequired incompatibility as material_ui above, one level
  # down (material_ui depends on this for adaptive Cupertino widgets).
  cupertino_ui: 1.0.2

  # Lightweight state management for the (dummy) player state.
  provider: ^6.1.2

  # Navidrome/Subsonic API client.
  http: ^1.2.2
  crypto: ^3.0.5

  # Persists the server URL / username / password locally between launches.
  shared_preferences: ^2.3.3

  # Real audio playback (streaming from Navidrome's /rest/stream.view).
  just_audio: ^0.9.42

  # Disk+memory caching for cover art, so repeat views don't re-download it.
  cached_network_image: ^3.4.1

  # App-private storage directory for downloaded (offline) song files.
  path_provider: ^2.1.5
  audio_service: ^0.18.19

  # Requests the Android 13+ runtime notification permission (without it,
  # the playback notification silently never shows). Pinned below 13.x:
  # permission_handler_android 14.x requires compileSdk 37, one version
  # ahead of what this project's Android Gradle Plugin currently supports.
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

# Generates the Android launcher icon, including the Android 13+ "themed"
# (dynamic color / Material You) monochrome variant. Re-run after changing
# any of the source images with: dart run flutter_launcher_icons
flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/icon/legacy.png"
  adaptive_icon_background: "#A66BA9"
  adaptive_icon_foreground: "assets/supwave.png"
  # supwave.png is already a plain white mark on transparent, so it doubles
  # as the monochrome (themed icon) layer with no separate file needed.
  adaptive_icon_monochrome: "assets/supwave.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # supwave.png (a plain white mark on transparent - see the icon config
  # above) is also used in-app on the login screen, tinted to match the
  # current color scheme.
  assets:
    - assets/supwave.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
